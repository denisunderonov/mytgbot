# Telegram Portfolio Bot 🤖

## Короткое описание

Простой Telegram-бот на TypeScript для представления портфолио. Использует библиотеку Grammy для работы с Telegram Bot API. Бот имеет интерактивное меню с кнопками для навигации по проектам и контактам.

## Возможности

✨ **Интерактивное меню** — inline-кнопки для навигации  
🚀 **Раздел "Проекты"** — показывает ссылки на ваши работы  
📱 **Раздел "Личный ТГК"** — ссылка на Telegram-канал  
⚡ **TypeScript** — типизация и современный код  
🔧 **Grammy** — современная библиотека для Telegram Bot API

## Технологии

- **Node.js** — серверная платформа
- **TypeScript** — типизированный JavaScript
- **Grammy** — фреймворк для Telegram ботов
- **dotenv** — управление переменными окружения

## Требования

- **Node.js** 18.x или выше
- **npm** или **yarn**
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## Установка

### 1. Клонировать репозиторий

```bash
git clone https://github.com/denisunderonov/mytgbot.git
cd mytgbot
```

### 2. Установить зависимости

```bash
npm install
```

### 3. Создать .env файл

Создайте файл `.env` в корне проекта:

```env
BOT_TOKEN=your_bot_token_here
```

**Как получить токен:**
1. Найдите в Telegram [@BotFather](https://t.me/BotFather)
2. Отправьте команду `/newbot`
3. Следуйте инструкциям (придумайте имя и username)
4. Скопируйте полученный токен в `.env`

## Запуск

### Режим разработки (с hot-reload)

```bash
npm run dev
```

Бот будет перезапускаться автоматически при изменении кода.

### Production

**1. Собрать проект:**

```bash
npm run build
```

**2. Запустить:**

```bash
npm start
```

## Структура проекта

```
mytgbot/
├── src/
│   └── index.ts          # Главный файл бота
├── dist/                 # Скомпилированный JS (создается при build)
├── .env                  # Переменные окружения (не коммитится)
├── .gitignore            # Игнорируемые файлы
├── package.json          # Зависимости и скрипты
├── tsconfig.json         # Конфигурация TypeScript
└── README.md             # Документация
```

## Функционал бота

### Команды:

- `/start` — главное меню с кнопками

### Кнопки:

- **"Проекты"** — показывает список проектов
- **"Личный ТГК"** — ссылка на Telegram-канал

## Кастомизация

### Изменить текст приветствия

Отредактируйте `src/index.ts`, найдите:

```typescript
await ctx.reply(
  "👋 Привет! Я тут немного в full-stack пытаюсь.\n\n" +
  "Вот примеры моих проектов. Выбери раздел:",
  // ...
);
```

### Добавить новую кнопку

```typescript
const keyboard = new InlineKeyboard()
  .text("Проекты", "projects")
  .text("Личный ТГК", "blog")
  .row()  // Новая строка кнопок
  .text("GitHub", "github")
  .text("Резюме", "resume");
```

Затем добавьте обработчик:

```typescript
bot.callbackQuery("github", async (ctx) => {
  await ctx.answerCallbackQuery();
  await ctx.editMessageText(
    "🔗 Мой GitHub:\n\n" +
    "https://github.com/denisunderonov"
  );
});
```

### Добавить ссылки на проекты

Найдите обработчик `"projects"` и добавьте ссылки:

```typescript
bot.callbackQuery("projects", async (ctx) => {
  await ctx.answerCallbackQuery();
  
  await ctx.editMessageText(
    "🚀 Мои проекты:\n\n" +
    "📝 [Notelist](https://github.com/denisunderonov/notelist)\n" +
    "📚 [Library System](https://github.com/denisunderonov/tablesEJS)\n" +
    "🧮 [Calculator](https://github.com/denisunderonov/calculatorVanilaJS)\n" +
    "🏗️ [Constructor](https://github.com/denisunderonov/constructorVanilaJS)",
    { parse_mode: "Markdown" }
  );
});
```

## Разработка

### Структура кода

```typescript
// 1. Импорты
import { Bot, InlineKeyboard } from "grammy";
import * as dotenv from "dotenv";

// 2. Инициализация
dotenv.config();
const bot = new Bot(process.env.BOT_TOKEN);

// 3. Команды
bot.command("start", async (ctx) => { /* ... */ });

// 4. Обработчики кнопок
bot.callbackQuery("название", async (ctx) => { /* ... */ });

// 5. Запуск бота
bot.start();
```

### Полезные ссылки Grammy

- [Документация Grammy](https://grammy.dev/)
- [API Reference](https://doc.deno.land/https://deno.land/x/grammy/mod.ts)
- [Примеры](https://github.com/grammyjs/examples)

## Деплой

### Локальный запуск (постоянно)

Используйте **PM2** или **systemd** для постоянной работы бота на сервере.

**PM2:**

```bash
npm install -g pm2
npm run build
pm2 start dist/index.js --name "tg-bot"
pm2 save
pm2 startup
```

### Cloud платформы

#### Railway.app / Render.com (рекомендуется)

1. Подключите GitHub репозиторий
2. Добавьте переменную окружения `BOT_TOKEN`
3. Build Command: `npm run build`
4. Start Command: `npm start`
5. Деплой!

#### Heroku

```bash
heroku create
heroku config:set BOT_TOKEN=your_token_here
git push heroku main
```

#### VPS (DigitalOcean, AWS, etc.)

```bash
# Клонировать на сервере
git clone https://github.com/denisunderonov/mytgbot.git
cd mytgbot

# Установить зависимости
npm install

# Создать .env с BOT_TOKEN
echo "BOT_TOKEN=your_token" > .env

# Собрать
npm run build

# Запустить с PM2
pm2 start npm --name "tg-bot" -- start
```

## Возможные улучшения

- [ ] Добавить базу данных (SQLite/PostgreSQL) для хранения данных пользователей
- [ ] Реализовать админ-панель для управления контентом
- [ ] Добавить команду `/help`
- [ ] Реализовать статистику использования
- [ ] Добавить логирование действий пользователей
- [ ] Мультиязычность (i18n)
- [ ] Inline-режим для быстрого доступа
- [ ] Webhook вместо polling для production

## Отладка

### Бот не запускается?

1. **Проверьте токен:**
   ```bash
   echo $BOT_TOKEN  # Должен вывести токен
   ```

2. **Проверьте .env файл:**
   ```bash
   cat .env
   # BOT_TOKEN=123456:ABC-DEF...
   ```

3. **Проверьте права доступа:**
   - Убедитесь, что бот не заблокирован в Telegram
   - Проверьте, что токен действителен

### Бот не отвечает?

1. Проверьте логи: `npm run dev` покажет ошибки
2. Убедитесь, что бот запущен
3. Попробуйте отправить `/start` снова

## Безопасность

⚠️ **Важно:**

1. **Никогда не коммитьте `.env` файл** — уже добавлен в `.gitignore`
2. **Держите токен в секрете** — не публикуйте его нигде
3. **Используйте переменные окружения** — для production
4. **Обновляйте зависимости** — `npm audit fix`

Если токен скомпрометирован:
1. Откройте [@BotFather](https://t.me/BotFather)
2. Отправьте `/mybots`
3. Выберите бота → API Token → Revoke current token

## Автор

Денис Андронов

## Лицензия

ISC
